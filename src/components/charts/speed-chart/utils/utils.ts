import * as satellite from 'satellite.js';
import { globalNow } from '@/components/utils/now';

export function getSatelliteSpeed(tleLine1:string, tleLine2: string, minutes = 100) {
    const results: {time: string; speed: number}[] = [];
    const now = globalNow;




     
}